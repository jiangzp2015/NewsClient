<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RadioGroup
        android:id="@+id/rg_tab_bottom"
        android:background="@drawable/bottom_tab_bg"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        >
        <RadioButton
            android:id="@+id/rb_content"
            android:checked="true"
            style="@style/BottomTab"
            android:text="首页"
            android:drawableTop="@drawable/home_selector"
            />
        <RadioButton
            android:id="@+id/rb_news"
            style="@style/BottomTab"
            android:text="新闻中心"
            android:drawableTop="@drawable/news_selector"
            />
        <RadioButton
            android:id="@+id/rb_smart"
            style="@style/BottomTab"
            android:text="智慧服务"
            android:drawableTop="@drawable/smart_selector"
            />
        <RadioButton
            android:id="@+id/rb_gov"
            style="@style/BottomTab"
            android:text="政务"
            android:drawableTop="@drawable/gov_selector"
            />
        <RadioButton
            android:id="@+id/rb_setting"
            style="@style/BottomTab"
            android:text="设置中心"
            android:drawableTop="@drawable/setting_selector"
            />

    </RadioGroup>
    <com.heima.newsclient.view.LazyViewPager
        android:id="@+id/vp_content"
        android:layout_above="@id/rg_tab_bottom"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></com.heima.newsclient.view.LazyViewPager>
</RelativeLayout>
